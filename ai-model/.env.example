# Path on the host machine to the folder with the GGUF file
MODEL_HOST_PATH=/path/to/models

# Optional overrides
# MODEL_FILENAME=vikhr-llama3.1-8b-instruct-r-21-09-24-q4_k_m.gguf

# --- CPU (see CPU.md) ---
# N_GPU_LAYERS=0
# N_CTX=2048
# N_THREADS=8
# N_THREADS_BATCH=8
# N_BATCH=512
# USE_MLOCK=1
# OMP_NUM_THREADS=8
# OPENBLAS_NUM_THREADS=8

# OpenBLAS image + tuning: docker compose -f docker-compose.yml -f docker-compose.cpu.yml up -d --build

# --- Local GPU (RTX 5060 8GB etc., see LOCAL-GPU.md) ---
# cp .env.local-gpu.example .env
# docker compose -f docker-compose.yml -f docker-compose.local-gpu.yml up -d --build

# --- Remote GPU server (see GPU.md) ---
# CUDA_WHEEL_TAG=cu124
