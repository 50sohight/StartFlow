# Database (used by docker-compose for Postgres and the backend service)
DB_NAME=startflow
DB_USER=startflow
DB_PASS=changeme

# JWT
JWT_SECRET_KEY=change-this-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_SECONDS=3600


PUBLIC_AI_URL=http://204.12.253.210:8077